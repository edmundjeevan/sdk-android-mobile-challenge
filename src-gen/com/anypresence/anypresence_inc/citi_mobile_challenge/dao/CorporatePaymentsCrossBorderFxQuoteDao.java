package com.anypresence.anypresence_inc.citi_mobile_challenge.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.anypresence.anypresence_inc.citi_mobile_challenge.dao.CorporatePaymentsCrossBorderFxQuote;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table CORPORATEPAYMENTSCROSSBORDERFXQUOTES.
 */
public class CorporatePaymentsCrossBorderFxQuoteDao
		extends
			AbstractDao<CorporatePaymentsCrossBorderFxQuote, Long> {

	public static final String TABLENAME = "CORPORATEPAYMENTSCROSSBORDERFXQUOTES";

	/**
	 * Properties of entity CorporatePaymentsCrossBorderFxQuote.<br/>
	 * Can be used for QueryBuilder and for referencing column names.
	 */
	public static class Properties {
		public final static Property Id = new Property(0, Long.class, "id",
				true, "_id");
		public final static Property RequestedOn = new Property(1,
				String.class, "requestedOn", false, "REQUESTED_ON");
		public final static Property FromAmount = new Property(2, Float.class,
				"fromAmount", false, "FROM_AMOUNT");
		public final static Property FromCurrency = new Property(3,
				String.class, "fromCurrency", false, "FROM_CURRENCY");
		public final static Property RateExpiry = new Property(4, String.class,
				"rateExpiry", false, "RATE_EXPIRY");
		public final static Property RateReference = new Property(5,
				String.class, "rateReference", false, "RATE_REFERENCE");
		public final static Property RequestedBy = new Property(6,
				String.class, "requestedBy", false, "REQUESTED_BY");
		public final static Property ToAmount = new Property(7, Float.class,
				"toAmount", false, "TO_AMOUNT");
		public final static Property ToCurrency = new Property(8, String.class,
				"toCurrency", false, "TO_CURRENCY");
		public final static Property ValueDate = new Property(9, String.class,
				"valueDate", false, "VALUE_DATE");
		public final static Property Scope = new Property(10, String.class,
				"scope", false, "SCOPE");
		public final static Property ObjectId = new Property(11, String.class,
				"objectId", false, "OBJECT_ID");
		public final static Property _cacheUpdatedAt = new Property(12,
				java.util.Date.class, "_cacheUpdatedAt", false,
				"_CACHE_UPDATED_AT");
		public final static Property ApCachedRequestId = new Property(13,
				Long.class, "apCachedRequestId", false, "AP_CACHED_REQUEST_ID");
	};

	private DaoSession daoSession;

	public CorporatePaymentsCrossBorderFxQuoteDao(DaoConfig config) {
		super(config);
	}

	public CorporatePaymentsCrossBorderFxQuoteDao(DaoConfig config,
			DaoSession daoSession) {
		super(config, daoSession);
		this.daoSession = daoSession;
	}

	/** Creates the underlying database table. */
	public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
		String constraint = ifNotExists ? "IF NOT EXISTS " : "";
		db.execSQL("CREATE TABLE " + constraint
				+ "'CORPORATEPAYMENTSCROSSBORDERFXQUOTES' (" + //
				"'_id' INTEGER PRIMARY KEY ," + // 0: id
				"'REQUESTED_ON' TEXT," + // 1: requestedOn
				"'FROM_AMOUNT' REAL," + // 2: fromAmount
				"'FROM_CURRENCY' TEXT," + // 3: fromCurrency
				"'RATE_EXPIRY' TEXT," + // 4: rateExpiry
				"'RATE_REFERENCE' TEXT," + // 5: rateReference
				"'REQUESTED_BY' TEXT," + // 6: requestedBy
				"'TO_AMOUNT' REAL," + // 7: toAmount
				"'TO_CURRENCY' TEXT," + // 8: toCurrency
				"'VALUE_DATE' TEXT," + // 9: valueDate
				"'SCOPE' TEXT," + // 10: scope
				"'OBJECT_ID' TEXT," + // 11: objectId
				"'_CACHE_UPDATED_AT' INTEGER," + // 12: _cacheUpdatedAt
				"'AP_CACHED_REQUEST_ID' INTEGER);"); // 13: apCachedRequestId
		// Add Indexes
		db.execSQL("CREATE UNIQUE INDEX "
				+ constraint
				+ "IDX_CORPORATEPAYMENTSCROSSBORDERFXQUOTES_OBJECT_ID ON CORPORATEPAYMENTSCROSSBORDERFXQUOTES"
				+ " (OBJECT_ID);");
	}

	/** Drops the underlying database table. */
	public static void dropTable(SQLiteDatabase db, boolean ifExists) {
		String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "")
				+ "'CORPORATEPAYMENTSCROSSBORDERFXQUOTES'";
		db.execSQL(sql);
	}

	/** @inheritdoc */
	@Override
	protected void bindValues(SQLiteStatement stmt,
			CorporatePaymentsCrossBorderFxQuote entity) {
		stmt.clearBindings();

		Long id = entity.getId();
		if (id != null) {
			stmt.bindLong(1, id);
		}

		String requestedOn = entity.getRequestedOn();
		if (requestedOn != null) {
			stmt.bindString(2, requestedOn);
		}

		Float fromAmount = entity.getFromAmount();
		if (fromAmount != null) {
			stmt.bindDouble(3, fromAmount);
		}

		String fromCurrency = entity.getFromCurrency();
		if (fromCurrency != null) {
			stmt.bindString(4, fromCurrency);
		}

		String rateExpiry = entity.getRateExpiry();
		if (rateExpiry != null) {
			stmt.bindString(5, rateExpiry);
		}

		String rateReference = entity.getRateReference();
		if (rateReference != null) {
			stmt.bindString(6, rateReference);
		}

		String requestedBy = entity.getRequestedBy();
		if (requestedBy != null) {
			stmt.bindString(7, requestedBy);
		}

		Float toAmount = entity.getToAmount();
		if (toAmount != null) {
			stmt.bindDouble(8, toAmount);
		}

		String toCurrency = entity.getToCurrency();
		if (toCurrency != null) {
			stmt.bindString(9, toCurrency);
		}

		String valueDate = entity.getValueDate();
		if (valueDate != null) {
			stmt.bindString(10, valueDate);
		}

		String scope = entity.getScope();
		if (scope != null) {
			stmt.bindString(11, scope);
		}

		String objectId = entity.getObjectId();
		if (objectId != null) {
			stmt.bindString(12, objectId);
		}

		java.util.Date _cacheUpdatedAt = entity.get_cacheUpdatedAt();
		if (_cacheUpdatedAt != null) {
			stmt.bindLong(13, _cacheUpdatedAt.getTime());
		}

		Long apCachedRequestId = entity.getApCachedRequestId();
		if (apCachedRequestId != null) {
			stmt.bindLong(14, apCachedRequestId);
		}
	}

	@Override
	protected void attachEntity(CorporatePaymentsCrossBorderFxQuote entity) {
		super.attachEntity(entity);
		entity.__setDaoSession(daoSession);
	}

	/** @inheritdoc */
	@Override
	public Long readKey(Cursor cursor, int offset) {
		return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
	}

	/** @inheritdoc */
	@Override
	public CorporatePaymentsCrossBorderFxQuote readEntity(Cursor cursor,
			int offset) {
		CorporatePaymentsCrossBorderFxQuote entity = new CorporatePaymentsCrossBorderFxQuote(
				//
				cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
				cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // requestedOn
				cursor.isNull(offset + 2) ? null : cursor.getFloat(offset + 2), // fromAmount
				cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // fromCurrency
				cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // rateExpiry
				cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // rateReference
				cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // requestedBy
				cursor.isNull(offset + 7) ? null : cursor.getFloat(offset + 7), // toAmount
				cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // toCurrency
				cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9), // valueDate
				cursor.isNull(offset + 10) ? null : cursor
						.getString(offset + 10), // scope
				cursor.isNull(offset + 11) ? null : cursor
						.getString(offset + 11), // objectId
				cursor.isNull(offset + 12) ? null : new java.util.Date(cursor
						.getLong(offset + 12)), // _cacheUpdatedAt
				cursor.isNull(offset + 13) ? null : cursor.getLong(offset + 13) // apCachedRequestId
		);
		return entity;
	}

	/** @inheritdoc */
	@Override
	public void readEntity(Cursor cursor,
			CorporatePaymentsCrossBorderFxQuote entity, int offset) {
		entity.setId(cursor.isNull(offset + 0) ? null : cursor
				.getLong(offset + 0));
		entity.setRequestedOn(cursor.isNull(offset + 1) ? null : cursor
				.getString(offset + 1));
		entity.setFromAmount(cursor.isNull(offset + 2) ? null : cursor
				.getFloat(offset + 2));
		entity.setFromCurrency(cursor.isNull(offset + 3) ? null : cursor
				.getString(offset + 3));
		entity.setRateExpiry(cursor.isNull(offset + 4) ? null : cursor
				.getString(offset + 4));
		entity.setRateReference(cursor.isNull(offset + 5) ? null : cursor
				.getString(offset + 5));
		entity.setRequestedBy(cursor.isNull(offset + 6) ? null : cursor
				.getString(offset + 6));
		entity.setToAmount(cursor.isNull(offset + 7) ? null : cursor
				.getFloat(offset + 7));
		entity.setToCurrency(cursor.isNull(offset + 8) ? null : cursor
				.getString(offset + 8));
		entity.setValueDate(cursor.isNull(offset + 9) ? null : cursor
				.getString(offset + 9));
		entity.setScope(cursor.isNull(offset + 10) ? null : cursor
				.getString(offset + 10));
		entity.setObjectId(cursor.isNull(offset + 11) ? null : cursor
				.getString(offset + 11));
		entity.set_cacheUpdatedAt(cursor.isNull(offset + 12)
				? null
				: new java.util.Date(cursor.getLong(offset + 12)));
		entity.setApCachedRequestId(cursor.isNull(offset + 13) ? null : cursor
				.getLong(offset + 13));
	}

	/** @inheritdoc */
	@Override
	protected Long updateKeyAfterInsert(
			CorporatePaymentsCrossBorderFxQuote entity, long rowId) {
		entity.setId(rowId);
		return rowId;
	}

	/** @inheritdoc */
	@Override
	public Long getKey(CorporatePaymentsCrossBorderFxQuote entity) {
		if (entity != null) {
			return entity.getId();
		} else {
			return null;
		}
	}

	/** @inheritdoc */
	@Override
	protected boolean isEntityUpdateable() {
		return true;
	}

	private String selectDeep;

	protected String getSelectDeep() {
		if (selectDeep == null) {
			StringBuilder builder = new StringBuilder("SELECT ");
			SqlUtils.appendColumns(builder, "T", getAllColumns());
			builder.append(',');
			SqlUtils.appendColumns(builder, "T0", daoSession
					.getAPCachedRequestDao().getAllColumns());
			builder.append(" FROM CORPORATEPAYMENTSCROSSBORDERFXQUOTES T");
			builder.append(" LEFT JOIN APCACHED_REQUEST T0 ON T.'AP_CACHED_REQUEST_ID'=T0.'_id'");
			builder.append(' ');
			selectDeep = builder.toString();
		}
		return selectDeep;
	}

	protected CorporatePaymentsCrossBorderFxQuote loadCurrentDeep(
			Cursor cursor, boolean lock) {
		CorporatePaymentsCrossBorderFxQuote entity = loadCurrent(cursor, 0,
				lock);
		int offset = getAllColumns().length;

		APCachedRequest aPCachedRequest = loadCurrentOther(
				daoSession.getAPCachedRequestDao(), cursor, offset);
		entity.setAPCachedRequest(aPCachedRequest);

		return entity;
	}

	public CorporatePaymentsCrossBorderFxQuote loadDeep(Long key) {
		assertSinglePk();
		if (key == null) {
			return null;
		}

		StringBuilder builder = new StringBuilder(getSelectDeep());
		builder.append("WHERE ");
		SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
		String sql = builder.toString();

		String[] keyArray = new String[]{key.toString()};
		Cursor cursor = db.rawQuery(sql, keyArray);

		try {
			boolean available = cursor.moveToFirst();
			if (!available) {
				return null;
			} else if (!cursor.isLast()) {
				throw new IllegalStateException(
						"Expected unique result, but count was "
								+ cursor.getCount());
			}
			return loadCurrentDeep(cursor, true);
		} finally {
			cursor.close();
		}
	}

	/** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
	public List<CorporatePaymentsCrossBorderFxQuote> loadAllDeepFromCursor(
			Cursor cursor) {
		int count = cursor.getCount();
		List<CorporatePaymentsCrossBorderFxQuote> list = new ArrayList<CorporatePaymentsCrossBorderFxQuote>(
				count);

		if (cursor.moveToFirst()) {
			if (identityScope != null) {
				identityScope.lock();
				identityScope.reserveRoom(count);
			}
			try {
				do {
					list.add(loadCurrentDeep(cursor, false));
				} while (cursor.moveToNext());
			} finally {
				if (identityScope != null) {
					identityScope.unlock();
				}
			}
		}
		return list;
	}

	protected List<CorporatePaymentsCrossBorderFxQuote> loadDeepAllAndCloseCursor(
			Cursor cursor) {
		try {
			return loadAllDeepFromCursor(cursor);
		} finally {
			cursor.close();
		}
	}

	/** A raw-style query where you can pass any WHERE clause and arguments. */
	public List<CorporatePaymentsCrossBorderFxQuote> queryDeep(String where,
			String... selectionArg) {
		Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
		return loadDeepAllAndCloseCursor(cursor);
	}

}
