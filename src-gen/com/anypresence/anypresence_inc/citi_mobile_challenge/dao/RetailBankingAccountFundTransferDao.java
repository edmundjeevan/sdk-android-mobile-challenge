package com.anypresence.anypresence_inc.citi_mobile_challenge.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.anypresence.anypresence_inc.citi_mobile_challenge.dao.RetailBankingAccountFundTransfer;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table RETAILBANKINGACCOUNTFUNDTRANSFERS.
 */
public class RetailBankingAccountFundTransferDao
		extends
			AbstractDao<RetailBankingAccountFundTransfer, Long> {

	public static final String TABLENAME = "RETAILBANKINGACCOUNTFUNDTRANSFERS";

	/**
	 * Properties of entity RetailBankingAccountFundTransfer.<br/>
	 * Can be used for QueryBuilder and for referencing column names.
	 */
	public static class Properties {
		public final static Property Id = new Property(0, Long.class, "id",
				true, "_id");
		public final static Property Amount = new Property(1, Float.class,
				"amount", false, "AMOUNT");
		public final static Property Currency = new Property(2, String.class,
				"currency", false, "CURRENCY");
		public final static Property DestinationAccountId = new Property(3,
				String.class, "destinationAccountId", false,
				"DESTINATION_ACCOUNT_ID");
		public final static Property DestinationId = new Property(4,
				String.class, "destinationId", false, "DESTINATION_ID");
		public final static Property Memo = new Property(5, String.class,
				"memo", false, "MEMO");
		public final static Property PayeeDesc = new Property(6, String.class,
				"payeeDesc", false, "PAYEE_DESC");
		public final static Property PayeeId = new Property(7, String.class,
				"payeeId", false, "PAYEE_ID");
		public final static Property PayeeType = new Property(8, String.class,
				"payeeType", false, "PAYEE_TYPE");
		public final static Property PaymentType = new Property(9,
				Integer.class, "paymentType", false, "PAYMENT_TYPE");
		public final static Property ReferenceNumber = new Property(10,
				String.class, "referenceNumber", false, "REFERENCE_NUMBER");
		public final static Property TransactionDate = new Property(11,
				String.class, "transactionDate", false, "TRANSACTION_DATE");
		public final static Property Scope = new Property(12, String.class,
				"scope", false, "SCOPE");
		public final static Property ObjectId = new Property(13, String.class,
				"objectId", false, "OBJECT_ID");
		public final static Property _cacheUpdatedAt = new Property(14,
				java.util.Date.class, "_cacheUpdatedAt", false,
				"_CACHE_UPDATED_AT");
		public final static Property ApCachedRequestId = new Property(15,
				Long.class, "apCachedRequestId", false, "AP_CACHED_REQUEST_ID");
	};

	private DaoSession daoSession;

	public RetailBankingAccountFundTransferDao(DaoConfig config) {
		super(config);
	}

	public RetailBankingAccountFundTransferDao(DaoConfig config,
			DaoSession daoSession) {
		super(config, daoSession);
		this.daoSession = daoSession;
	}

	/** Creates the underlying database table. */
	public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
		String constraint = ifNotExists ? "IF NOT EXISTS " : "";
		db.execSQL("CREATE TABLE " + constraint
				+ "'RETAILBANKINGACCOUNTFUNDTRANSFERS' (" + //
				"'_id' INTEGER PRIMARY KEY ," + // 0: id
				"'AMOUNT' REAL," + // 1: amount
				"'CURRENCY' TEXT," + // 2: currency
				"'DESTINATION_ACCOUNT_ID' TEXT," + // 3: destinationAccountId
				"'DESTINATION_ID' TEXT," + // 4: destinationId
				"'MEMO' TEXT," + // 5: memo
				"'PAYEE_DESC' TEXT," + // 6: payeeDesc
				"'PAYEE_ID' TEXT," + // 7: payeeId
				"'PAYEE_TYPE' TEXT," + // 8: payeeType
				"'PAYMENT_TYPE' INTEGER," + // 9: paymentType
				"'REFERENCE_NUMBER' TEXT," + // 10: referenceNumber
				"'TRANSACTION_DATE' TEXT," + // 11: transactionDate
				"'SCOPE' TEXT," + // 12: scope
				"'OBJECT_ID' TEXT," + // 13: objectId
				"'_CACHE_UPDATED_AT' INTEGER," + // 14: _cacheUpdatedAt
				"'AP_CACHED_REQUEST_ID' INTEGER);"); // 15: apCachedRequestId
		// Add Indexes
		db.execSQL("CREATE UNIQUE INDEX "
				+ constraint
				+ "IDX_RETAILBANKINGACCOUNTFUNDTRANSFERS_OBJECT_ID ON RETAILBANKINGACCOUNTFUNDTRANSFERS"
				+ " (OBJECT_ID);");
	}

	/** Drops the underlying database table. */
	public static void dropTable(SQLiteDatabase db, boolean ifExists) {
		String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "")
				+ "'RETAILBANKINGACCOUNTFUNDTRANSFERS'";
		db.execSQL(sql);
	}

	/** @inheritdoc */
	@Override
	protected void bindValues(SQLiteStatement stmt,
			RetailBankingAccountFundTransfer entity) {
		stmt.clearBindings();

		Long id = entity.getId();
		if (id != null) {
			stmt.bindLong(1, id);
		}

		Float amount = entity.getAmount();
		if (amount != null) {
			stmt.bindDouble(2, amount);
		}

		String currency = entity.getCurrency();
		if (currency != null) {
			stmt.bindString(3, currency);
		}

		String destinationAccountId = entity.getDestinationAccountId();
		if (destinationAccountId != null) {
			stmt.bindString(4, destinationAccountId);
		}

		String destinationId = entity.getDestinationId();
		if (destinationId != null) {
			stmt.bindString(5, destinationId);
		}

		String memo = entity.getMemo();
		if (memo != null) {
			stmt.bindString(6, memo);
		}

		String payeeDesc = entity.getPayeeDesc();
		if (payeeDesc != null) {
			stmt.bindString(7, payeeDesc);
		}

		String payeeId = entity.getPayeeId();
		if (payeeId != null) {
			stmt.bindString(8, payeeId);
		}

		String payeeType = entity.getPayeeType();
		if (payeeType != null) {
			stmt.bindString(9, payeeType);
		}

		Integer paymentType = entity.getPaymentType();
		if (paymentType != null) {
			stmt.bindLong(10, paymentType);
		}

		String referenceNumber = entity.getReferenceNumber();
		if (referenceNumber != null) {
			stmt.bindString(11, referenceNumber);
		}

		String transactionDate = entity.getTransactionDate();
		if (transactionDate != null) {
			stmt.bindString(12, transactionDate);
		}

		String scope = entity.getScope();
		if (scope != null) {
			stmt.bindString(13, scope);
		}

		String objectId = entity.getObjectId();
		if (objectId != null) {
			stmt.bindString(14, objectId);
		}

		java.util.Date _cacheUpdatedAt = entity.get_cacheUpdatedAt();
		if (_cacheUpdatedAt != null) {
			stmt.bindLong(15, _cacheUpdatedAt.getTime());
		}

		Long apCachedRequestId = entity.getApCachedRequestId();
		if (apCachedRequestId != null) {
			stmt.bindLong(16, apCachedRequestId);
		}
	}

	@Override
	protected void attachEntity(RetailBankingAccountFundTransfer entity) {
		super.attachEntity(entity);
		entity.__setDaoSession(daoSession);
	}

	/** @inheritdoc */
	@Override
	public Long readKey(Cursor cursor, int offset) {
		return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
	}

	/** @inheritdoc */
	@Override
	public RetailBankingAccountFundTransfer readEntity(Cursor cursor, int offset) {
		RetailBankingAccountFundTransfer entity = new RetailBankingAccountFundTransfer(
				//
				cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
				cursor.isNull(offset + 1) ? null : cursor.getFloat(offset + 1), // amount
				cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // currency
				cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // destinationAccountId
				cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // destinationId
				cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // memo
				cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // payeeDesc
				cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // payeeId
				cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // payeeType
				cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9), // paymentType
				cursor.isNull(offset + 10) ? null : cursor
						.getString(offset + 10), // referenceNumber
				cursor.isNull(offset + 11) ? null : cursor
						.getString(offset + 11), // transactionDate
				cursor.isNull(offset + 12) ? null : cursor
						.getString(offset + 12), // scope
				cursor.isNull(offset + 13) ? null : cursor
						.getString(offset + 13), // objectId
				cursor.isNull(offset + 14) ? null : new java.util.Date(cursor
						.getLong(offset + 14)), // _cacheUpdatedAt
				cursor.isNull(offset + 15) ? null : cursor.getLong(offset + 15) // apCachedRequestId
		);
		return entity;
	}

	/** @inheritdoc */
	@Override
	public void readEntity(Cursor cursor,
			RetailBankingAccountFundTransfer entity, int offset) {
		entity.setId(cursor.isNull(offset + 0) ? null : cursor
				.getLong(offset + 0));
		entity.setAmount(cursor.isNull(offset + 1) ? null : cursor
				.getFloat(offset + 1));
		entity.setCurrency(cursor.isNull(offset + 2) ? null : cursor
				.getString(offset + 2));
		entity.setDestinationAccountId(cursor.isNull(offset + 3)
				? null
				: cursor.getString(offset + 3));
		entity.setDestinationId(cursor.isNull(offset + 4) ? null : cursor
				.getString(offset + 4));
		entity.setMemo(cursor.isNull(offset + 5) ? null : cursor
				.getString(offset + 5));
		entity.setPayeeDesc(cursor.isNull(offset + 6) ? null : cursor
				.getString(offset + 6));
		entity.setPayeeId(cursor.isNull(offset + 7) ? null : cursor
				.getString(offset + 7));
		entity.setPayeeType(cursor.isNull(offset + 8) ? null : cursor
				.getString(offset + 8));
		entity.setPaymentType(cursor.isNull(offset + 9) ? null : cursor
				.getInt(offset + 9));
		entity.setReferenceNumber(cursor.isNull(offset + 10) ? null : cursor
				.getString(offset + 10));
		entity.setTransactionDate(cursor.isNull(offset + 11) ? null : cursor
				.getString(offset + 11));
		entity.setScope(cursor.isNull(offset + 12) ? null : cursor
				.getString(offset + 12));
		entity.setObjectId(cursor.isNull(offset + 13) ? null : cursor
				.getString(offset + 13));
		entity.set_cacheUpdatedAt(cursor.isNull(offset + 14)
				? null
				: new java.util.Date(cursor.getLong(offset + 14)));
		entity.setApCachedRequestId(cursor.isNull(offset + 15) ? null : cursor
				.getLong(offset + 15));
	}

	/** @inheritdoc */
	@Override
	protected Long updateKeyAfterInsert(
			RetailBankingAccountFundTransfer entity, long rowId) {
		entity.setId(rowId);
		return rowId;
	}

	/** @inheritdoc */
	@Override
	public Long getKey(RetailBankingAccountFundTransfer entity) {
		if (entity != null) {
			return entity.getId();
		} else {
			return null;
		}
	}

	/** @inheritdoc */
	@Override
	protected boolean isEntityUpdateable() {
		return true;
	}

	private String selectDeep;

	protected String getSelectDeep() {
		if (selectDeep == null) {
			StringBuilder builder = new StringBuilder("SELECT ");
			SqlUtils.appendColumns(builder, "T", getAllColumns());
			builder.append(',');
			SqlUtils.appendColumns(builder, "T0", daoSession
					.getAPCachedRequestDao().getAllColumns());
			builder.append(" FROM RETAILBANKINGACCOUNTFUNDTRANSFERS T");
			builder.append(" LEFT JOIN APCACHED_REQUEST T0 ON T.'AP_CACHED_REQUEST_ID'=T0.'_id'");
			builder.append(' ');
			selectDeep = builder.toString();
		}
		return selectDeep;
	}

	protected RetailBankingAccountFundTransfer loadCurrentDeep(Cursor cursor,
			boolean lock) {
		RetailBankingAccountFundTransfer entity = loadCurrent(cursor, 0, lock);
		int offset = getAllColumns().length;

		APCachedRequest aPCachedRequest = loadCurrentOther(
				daoSession.getAPCachedRequestDao(), cursor, offset);
		entity.setAPCachedRequest(aPCachedRequest);

		return entity;
	}

	public RetailBankingAccountFundTransfer loadDeep(Long key) {
		assertSinglePk();
		if (key == null) {
			return null;
		}

		StringBuilder builder = new StringBuilder(getSelectDeep());
		builder.append("WHERE ");
		SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
		String sql = builder.toString();

		String[] keyArray = new String[]{key.toString()};
		Cursor cursor = db.rawQuery(sql, keyArray);

		try {
			boolean available = cursor.moveToFirst();
			if (!available) {
				return null;
			} else if (!cursor.isLast()) {
				throw new IllegalStateException(
						"Expected unique result, but count was "
								+ cursor.getCount());
			}
			return loadCurrentDeep(cursor, true);
		} finally {
			cursor.close();
		}
	}

	/** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
	public List<RetailBankingAccountFundTransfer> loadAllDeepFromCursor(
			Cursor cursor) {
		int count = cursor.getCount();
		List<RetailBankingAccountFundTransfer> list = new ArrayList<RetailBankingAccountFundTransfer>(
				count);

		if (cursor.moveToFirst()) {
			if (identityScope != null) {
				identityScope.lock();
				identityScope.reserveRoom(count);
			}
			try {
				do {
					list.add(loadCurrentDeep(cursor, false));
				} while (cursor.moveToNext());
			} finally {
				if (identityScope != null) {
					identityScope.unlock();
				}
			}
		}
		return list;
	}

	protected List<RetailBankingAccountFundTransfer> loadDeepAllAndCloseCursor(
			Cursor cursor) {
		try {
			return loadAllDeepFromCursor(cursor);
		} finally {
			cursor.close();
		}
	}

	/** A raw-style query where you can pass any WHERE clause and arguments. */
	public List<RetailBankingAccountFundTransfer> queryDeep(String where,
			String... selectionArg) {
		Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
		return loadDeepAllAndCloseCursor(cursor);
	}

}
