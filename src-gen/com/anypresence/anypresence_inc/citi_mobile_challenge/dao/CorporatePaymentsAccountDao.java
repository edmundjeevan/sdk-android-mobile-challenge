package com.anypresence.anypresence_inc.citi_mobile_challenge.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.anypresence.anypresence_inc.citi_mobile_challenge.dao.CorporatePaymentsAccount;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table CORPORATEPAYMENTSACCOUNTS.
 */
public class CorporatePaymentsAccountDao
		extends
			AbstractDao<CorporatePaymentsAccount, Long> {

	public static final String TABLENAME = "CORPORATEPAYMENTSACCOUNTS";

	/**
	 * Properties of entity CorporatePaymentsAccount.<br/>
	 * Can be used for QueryBuilder and for referencing column names.
	 */
	public static class Properties {
		public final static Property Id = new Property(0, Long.class, "id",
				true, "_id");
		public final static Property AccountName = new Property(1,
				String.class, "accountName", false, "ACCOUNT_NAME");
		public final static Property BaseCurrencyBalances = new Property(2,
				String.class, "baseCurrencyBalances", false,
				"BASE_CURRENCY_BALANCES");
		public final static Property BranchName = new Property(3, String.class,
				"branchName", false, "BRANCH_NAME");
		public final static Property CountryIsoCode = new Property(4,
				String.class, "countryIsoCode", false, "COUNTRY_ISO_CODE");
		public final static Property LocalCurrencyIsoCode = new Property(5,
				String.class, "localCurrencyIsoCode", false,
				"LOCAL_CURRENCY_ISO_CODE");
		public final static Property Name = new Property(6, String.class,
				"name", false, "NAME");
		public final static Property Status = new Property(7, String.class,
				"status", false, "STATUS");
		public final static Property Scope = new Property(8, String.class,
				"scope", false, "SCOPE");
		public final static Property ObjectId = new Property(9, String.class,
				"objectId", false, "OBJECT_ID");
		public final static Property _cacheUpdatedAt = new Property(10,
				java.util.Date.class, "_cacheUpdatedAt", false,
				"_CACHE_UPDATED_AT");
		public final static Property ApCachedRequestId = new Property(11,
				Long.class, "apCachedRequestId", false, "AP_CACHED_REQUEST_ID");
	};

	private DaoSession daoSession;

	public CorporatePaymentsAccountDao(DaoConfig config) {
		super(config);
	}

	public CorporatePaymentsAccountDao(DaoConfig config, DaoSession daoSession) {
		super(config, daoSession);
		this.daoSession = daoSession;
	}

	/** Creates the underlying database table. */
	public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
		String constraint = ifNotExists ? "IF NOT EXISTS " : "";
		db.execSQL("CREATE TABLE " + constraint
				+ "'CORPORATEPAYMENTSACCOUNTS' (" + //
				"'_id' INTEGER PRIMARY KEY ," + // 0: id
				"'ACCOUNT_NAME' TEXT," + // 1: accountName
				"'BASE_CURRENCY_BALANCES' TEXT," + // 2: baseCurrencyBalances
				"'BRANCH_NAME' TEXT," + // 3: branchName
				"'COUNTRY_ISO_CODE' TEXT," + // 4: countryIsoCode
				"'LOCAL_CURRENCY_ISO_CODE' TEXT," + // 5: localCurrencyIsoCode
				"'NAME' TEXT," + // 6: name
				"'STATUS' TEXT," + // 7: status
				"'SCOPE' TEXT," + // 8: scope
				"'OBJECT_ID' TEXT," + // 9: objectId
				"'_CACHE_UPDATED_AT' INTEGER," + // 10: _cacheUpdatedAt
				"'AP_CACHED_REQUEST_ID' INTEGER);"); // 11: apCachedRequestId
		// Add Indexes
		db.execSQL("CREATE UNIQUE INDEX "
				+ constraint
				+ "IDX_CORPORATEPAYMENTSACCOUNTS_OBJECT_ID ON CORPORATEPAYMENTSACCOUNTS"
				+ " (OBJECT_ID);");
	}

	/** Drops the underlying database table. */
	public static void dropTable(SQLiteDatabase db, boolean ifExists) {
		String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "")
				+ "'CORPORATEPAYMENTSACCOUNTS'";
		db.execSQL(sql);
	}

	/** @inheritdoc */
	@Override
	protected void bindValues(SQLiteStatement stmt,
			CorporatePaymentsAccount entity) {
		stmt.clearBindings();

		Long id = entity.getId();
		if (id != null) {
			stmt.bindLong(1, id);
		}

		String accountName = entity.getAccountName();
		if (accountName != null) {
			stmt.bindString(2, accountName);
		}

		String baseCurrencyBalances = entity.getBaseCurrencyBalances();
		if (baseCurrencyBalances != null) {
			stmt.bindString(3, baseCurrencyBalances);
		}

		String branchName = entity.getBranchName();
		if (branchName != null) {
			stmt.bindString(4, branchName);
		}

		String countryIsoCode = entity.getCountryIsoCode();
		if (countryIsoCode != null) {
			stmt.bindString(5, countryIsoCode);
		}

		String localCurrencyIsoCode = entity.getLocalCurrencyIsoCode();
		if (localCurrencyIsoCode != null) {
			stmt.bindString(6, localCurrencyIsoCode);
		}

		String name = entity.getName();
		if (name != null) {
			stmt.bindString(7, name);
		}

		String status = entity.getStatus();
		if (status != null) {
			stmt.bindString(8, status);
		}

		String scope = entity.getScope();
		if (scope != null) {
			stmt.bindString(9, scope);
		}

		String objectId = entity.getObjectId();
		if (objectId != null) {
			stmt.bindString(10, objectId);
		}

		java.util.Date _cacheUpdatedAt = entity.get_cacheUpdatedAt();
		if (_cacheUpdatedAt != null) {
			stmt.bindLong(11, _cacheUpdatedAt.getTime());
		}

		Long apCachedRequestId = entity.getApCachedRequestId();
		if (apCachedRequestId != null) {
			stmt.bindLong(12, apCachedRequestId);
		}
	}

	@Override
	protected void attachEntity(CorporatePaymentsAccount entity) {
		super.attachEntity(entity);
		entity.__setDaoSession(daoSession);
	}

	/** @inheritdoc */
	@Override
	public Long readKey(Cursor cursor, int offset) {
		return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
	}

	/** @inheritdoc */
	@Override
	public CorporatePaymentsAccount readEntity(Cursor cursor, int offset) {
		CorporatePaymentsAccount entity = new CorporatePaymentsAccount(
				//
				cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
				cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // accountName
				cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // baseCurrencyBalances
				cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // branchName
				cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // countryIsoCode
				cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // localCurrencyIsoCode
				cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // name
				cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // status
				cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // scope
				cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9), // objectId
				cursor.isNull(offset + 10) ? null : new java.util.Date(cursor
						.getLong(offset + 10)), // _cacheUpdatedAt
				cursor.isNull(offset + 11) ? null : cursor.getLong(offset + 11) // apCachedRequestId
		);
		return entity;
	}

	/** @inheritdoc */
	@Override
	public void readEntity(Cursor cursor, CorporatePaymentsAccount entity,
			int offset) {
		entity.setId(cursor.isNull(offset + 0) ? null : cursor
				.getLong(offset + 0));
		entity.setAccountName(cursor.isNull(offset + 1) ? null : cursor
				.getString(offset + 1));
		entity.setBaseCurrencyBalances(cursor.isNull(offset + 2)
				? null
				: cursor.getString(offset + 2));
		entity.setBranchName(cursor.isNull(offset + 3) ? null : cursor
				.getString(offset + 3));
		entity.setCountryIsoCode(cursor.isNull(offset + 4) ? null : cursor
				.getString(offset + 4));
		entity.setLocalCurrencyIsoCode(cursor.isNull(offset + 5)
				? null
				: cursor.getString(offset + 5));
		entity.setName(cursor.isNull(offset + 6) ? null : cursor
				.getString(offset + 6));
		entity.setStatus(cursor.isNull(offset + 7) ? null : cursor
				.getString(offset + 7));
		entity.setScope(cursor.isNull(offset + 8) ? null : cursor
				.getString(offset + 8));
		entity.setObjectId(cursor.isNull(offset + 9) ? null : cursor
				.getString(offset + 9));
		entity.set_cacheUpdatedAt(cursor.isNull(offset + 10)
				? null
				: new java.util.Date(cursor.getLong(offset + 10)));
		entity.setApCachedRequestId(cursor.isNull(offset + 11) ? null : cursor
				.getLong(offset + 11));
	}

	/** @inheritdoc */
	@Override
	protected Long updateKeyAfterInsert(CorporatePaymentsAccount entity,
			long rowId) {
		entity.setId(rowId);
		return rowId;
	}

	/** @inheritdoc */
	@Override
	public Long getKey(CorporatePaymentsAccount entity) {
		if (entity != null) {
			return entity.getId();
		} else {
			return null;
		}
	}

	/** @inheritdoc */
	@Override
	protected boolean isEntityUpdateable() {
		return true;
	}

	private String selectDeep;

	protected String getSelectDeep() {
		if (selectDeep == null) {
			StringBuilder builder = new StringBuilder("SELECT ");
			SqlUtils.appendColumns(builder, "T", getAllColumns());
			builder.append(',');
			SqlUtils.appendColumns(builder, "T0", daoSession
					.getAPCachedRequestDao().getAllColumns());
			builder.append(" FROM CORPORATEPAYMENTSACCOUNTS T");
			builder.append(" LEFT JOIN APCACHED_REQUEST T0 ON T.'AP_CACHED_REQUEST_ID'=T0.'_id'");
			builder.append(' ');
			selectDeep = builder.toString();
		}
		return selectDeep;
	}

	protected CorporatePaymentsAccount loadCurrentDeep(Cursor cursor,
			boolean lock) {
		CorporatePaymentsAccount entity = loadCurrent(cursor, 0, lock);
		int offset = getAllColumns().length;

		APCachedRequest aPCachedRequest = loadCurrentOther(
				daoSession.getAPCachedRequestDao(), cursor, offset);
		entity.setAPCachedRequest(aPCachedRequest);

		return entity;
	}

	public CorporatePaymentsAccount loadDeep(Long key) {
		assertSinglePk();
		if (key == null) {
			return null;
		}

		StringBuilder builder = new StringBuilder(getSelectDeep());
		builder.append("WHERE ");
		SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
		String sql = builder.toString();

		String[] keyArray = new String[]{key.toString()};
		Cursor cursor = db.rawQuery(sql, keyArray);

		try {
			boolean available = cursor.moveToFirst();
			if (!available) {
				return null;
			} else if (!cursor.isLast()) {
				throw new IllegalStateException(
						"Expected unique result, but count was "
								+ cursor.getCount());
			}
			return loadCurrentDeep(cursor, true);
		} finally {
			cursor.close();
		}
	}

	/** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
	public List<CorporatePaymentsAccount> loadAllDeepFromCursor(Cursor cursor) {
		int count = cursor.getCount();
		List<CorporatePaymentsAccount> list = new ArrayList<CorporatePaymentsAccount>(
				count);

		if (cursor.moveToFirst()) {
			if (identityScope != null) {
				identityScope.lock();
				identityScope.reserveRoom(count);
			}
			try {
				do {
					list.add(loadCurrentDeep(cursor, false));
				} while (cursor.moveToNext());
			} finally {
				if (identityScope != null) {
					identityScope.unlock();
				}
			}
		}
		return list;
	}

	protected List<CorporatePaymentsAccount> loadDeepAllAndCloseCursor(
			Cursor cursor) {
		try {
			return loadAllDeepFromCursor(cursor);
		} finally {
			cursor.close();
		}
	}

	/** A raw-style query where you can pass any WHERE clause and arguments. */
	public List<CorporatePaymentsAccount> queryDeep(String where,
			String... selectionArg) {
		Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
		return loadDeepAllAndCloseCursor(cursor);
	}

}
